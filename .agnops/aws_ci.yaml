workflow:
  autoTrigger: true
  branchFilters:
    - develop
  containers:
    - container:
      addOns:
        isDocker: true
      kubernetes:
        envFrom:
          - secretRef:
              name: docker-registry-creds
      name: docker
      image: docker:18.09.9
      command: |
        echo "$DOCKERHUBPASS" | docker login --username $DOCKERHUBUSER --password-stdin
        docker build -f aws.Dockerfile -t agnops/cloud-wrapper:aws-latest .
        docker tag agnops/cloud-wrapper:aws-latest agnops/cloud-wrapper:aws-$COMMITID
        docker push agnops/cloud-wrapper:aws-latest && docker push agnops/cloud-wrapper:aws-$COMMITID